import { type Maybe, type _, make } from "momotaro/core/core"
import { Vector, VectorElement } from "momotaro/components/collections/vector";

export async function signin_form() {
	const vec = new Vector("signin-form", {} as JSON);
}
